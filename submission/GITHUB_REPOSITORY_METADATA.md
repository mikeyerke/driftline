# GitHub repository metadata

Status: prepared locally. Applying any value below changes the public
repository and requires explicit publication authorization.

## About

- **Description:** Google ADK + Gemini control plane that turns conflicting
  product evidence into a human-approved reversible experiment and reopens the
  decision when a guardrail breaks.
- **Website:** https://driftline-ops.web.app/
- **Topics:** `google-adk`, `gemini`, `google-cloud`, `cloud-run`, `firestore`,
  `bigquery`, `cloud-tasks`, `ai-agents`, `multi-agent`, `product-management`,
  `human-in-the-loop`, `hackathon`

The current public repository has no description, website, or topics. Do not
apply this metadata until the candidate has merged and the hosted URL is
reverified against that exact public commit.

## Final repository review

1. Confirm the README's release truth matches `/health`, Cloud Run, Cloud Build,
   Artifact Registry, and public `main`.
2. Confirm the README's first screen shows the live URL, 60-second judge path,
   current architecture, and explicit unreleased-candidate boundary.
3. Apply the description, website, and topics above in GitHub's **About** panel.
4. Open the repository logged out and test every first-screen link.
5. Confirm the default branch is `main`, the MIT license renders, the latest
   required checks pass, and no local rehearsal MP4 entered Git history.
6. Capture the final public repository URL and exact commit in the submission
   record.
